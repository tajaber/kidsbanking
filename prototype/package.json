{
  "name": "kidsbank-prototype",
  "version": "4.0.0",
  "private": true,
  "description": "KidsBank clickable HTML/CSS/JS prototype (offline, no build). The app itself needs no dependencies; jsdom is only used for the headless smoke test.",
  "scripts": {
    "test": "node test/smoke.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
